{
	"$schema": "http://json-schema.org/draft/2019-09/schema#",
	"type": "object",
	"title": "osiota application debug-output",
	"description": "This application can be used to output a string to the console for debugging purposes.",
	"properties": {
		"text": {
			"type": "string",
			"description": "Debug output text",
			"examples": [
				"Hello World!"
			]
		}
	}
}
